<settings>
    <servers>
        <server>
            <id>github</id>
            <username>${env.GITHUB_ACTOR}</username>
            <password>${env.GITHUB_TOKEN}</password>
        </server>
        <server>
            <id>videoplaza/avro-message-batch</id>
            <username>${env.GITHUB_ACTOR}</username>
            <password>${env.AVRO_MESSAGE_BATCH_TOKEN}</password>
        </server>
        <server>
            <id>artifactory</id>
            <username>${env.ARTIFACTORY_USER}</username>
            <password>${env.ARTIFACTORY_PASSWORD}</password>
        </server>
    </servers>
</settings>